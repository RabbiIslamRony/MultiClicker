// ================================================================
//  MULTICLICKER EXTENSION — DEFAULT CONFIGURATION
//  This config is seeded on first install.
//  Credentials are intentionally blank — fill them in Settings.
// ================================================================

const DEFAULT_CONFIG = {
  categories: [],
};
